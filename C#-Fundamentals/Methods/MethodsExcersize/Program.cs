using System;
using System.Linq;

namespace MethodsExcersize
{
    class Program
    {
        static void Main(string[] args)
        {
            double[] numbers = Console.ReadLine()
                .Split()
                .Select(double.Parse)
                .ToArray();

            foreach (var n in collection)
            {

            }
        }
    }
}
